# AI Coding Services

Standalone service extraction from AIMonitor's non-UI architecture.

This repository starts with the shared core, workflow, MSBuild, indexing, data, logging, runtime, MCP server, MCP stdio bridge, and extracted MCP hub service. The first validation lane is the unit suite plus the language corpus smoke tests.
